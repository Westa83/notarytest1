---
import "../styles/tailwind.css";
---

<html lang="pl" class="scroll-smooth">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&family=Source+Sans+3:wght@400;500;600&display=swap"
			rel="stylesheet"
		/>
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<meta name="description" content="Kancelaria notarialna — pewność i zaufanie. Czynności notarialne, akty, pełnomocnictwa. Bezpieczeństwo prawne i indywidualne podejście. Umów wizytę." />
		<meta name="generator" content={Astro.generator} />
		<title>Kancelaria notarialna — pewność i zaufanie</title>
	</head>
	<body
		class="overflow-x-hidden bg-[#f7f7f9] font-['Source_Sans_3',Inter,-apple-system,BlinkMacSystemFont,'Segoe_UI',sans-serif] text-base leading-relaxed text-[#44444a]"
	>
		<header class="sticky top-0 z-10 border-b border-[#e5e5ea] bg-white">
			<nav
				class="mx-auto grid w-full max-w-[1200px] grid-cols-[1fr_auto] items-center gap-3 p-4 sm:p-5 md:grid-cols-[auto_1fr_auto] md:gap-6 md:px-7 md:py-[22px] lg:gap-8 lg:px-8 lg:py-6 2xl:px-10"
				aria-label="Nawigacja główna"
			>
				<strong
					class="font-['Playfair_Display','Cormorant_Garamond',Georgia,serif] text-lg font-semibold leading-tight text-[#2b2b2f]"
				>
					Kancelaria notarialna
				</strong>
				<button
					class="menu-toggle inline-flex min-h-[44px] min-w-[44px] flex-col justify-center gap-1.5 rounded-md border border-[#e5e5ea] bg-white p-2 text-[#2b2b2f] focus:outline-none focus:ring-2 focus:ring-[#7b6886] focus:ring-offset-2 md:hidden"
					type="button"
					aria-expanded="false"
					aria-controls="main-menu"
					aria-label="Otwórz menu"
				>
					<span aria-hidden="true" class="h-0.5 w-[22px] bg-current"></span>
					<span aria-hidden="true" class="h-0.5 w-[22px] bg-current"></span>
					<span aria-hidden="true" class="h-0.5 w-[22px] bg-current"></span>
				</button>
				<div
					id="main-menu"
					class="menu-links col-span-full hidden flex-col items-stretch gap-2 pt-1 md:col-auto md:flex md:flex-row md:items-center md:justify-center md:gap-5 md:pt-0 lg:gap-8"
				>
					<a
						class="menu-link inline-flex min-h-[44px] min-w-[44px] items-center justify-center border-b-2 border-transparent px-3 py-2.5 text-base font-medium text-[#2b2b2f] transition-colors focus:outline-none focus:ring-2 focus:ring-[#7b6886] focus:ring-inset rounded"
						href="#o-nas">O nas</a
					>
					<a
						class="menu-link inline-flex min-h-[44px] min-w-[44px] items-center justify-center border-b-2 border-transparent px-3 py-2.5 text-base font-medium text-[#2b2b2f] transition-colors focus:outline-none focus:ring-2 focus:ring-[#7b6886] focus:ring-inset rounded"
						href="#uslugi">Usługi</a
					>
					<a
						class="menu-link inline-flex min-h-[44px] min-w-[44px] items-center justify-center border-b-2 border-transparent px-3 py-2.5 text-base font-medium text-[#2b2b2f] transition-colors focus:outline-none focus:ring-2 focus:ring-[#7b6886] focus:ring-inset rounded"
						href="#kontakt">Kontakt</a
					>
				</div>
				<a
					class="hidden min-h-[44px] min-w-[44px] items-center justify-center rounded-md bg-[#7b6886] px-5 py-2.5 text-base font-semibold tracking-[0.3px] text-white transition-colors hover:bg-[#665571] focus:outline-none focus:ring-2 focus:ring-[#7b6886] focus:ring-offset-2 md:col-auto md:inline-flex md:justify-self-end md:px-6"
					href="#kontakt"
				>
					Umów wizytę
				</a>
			</nav>
		</header>

		<main>
			<section
				class="bg-[#F7F7F9] border-b border-[#E5E5EA] py-16 sm:py-20 md:py-24 lg:py-[120px]"
				aria-labelledby="hero-heading"
			>
				<div class="mx-auto w-full max-w-[1200px] px-4 sm:px-5 md:px-7 lg:px-8 2xl:px-10">
					<div class="mx-auto max-w-[42rem] text-center">
						<h1
							id="hero-heading"
							class="font-['Playfair_Display','Cormorant_Garamond',Georgia,serif] text-[32px] font-semibold leading-[1.2] tracking-[-0.3px] text-[#2B2B2F] sm:text-[40px] md:text-5xl"
						>
							Kancelaria notarialna — pewność i zaufanie
						</h1>
						<p class="mt-4 text-lg leading-[1.6] text-[#44444A] sm:mt-5 sm:text-[18px] md:mt-6">
							Świadczymy czynności notarialne z pełną starannością. Bezpieczeństwo prawne, dyskrecja i indywidualne podejście do każdej sprawy.
						</p>
						<p class="mt-6 sm:mt-8">
							<a
								class="inline-flex min-h-[44px] min-w-[44px] items-center justify-center rounded bg-[#7B6886] px-7 py-3.5 text-base font-medium tracking-[0.3px] text-white transition-[background-color] duration-200 ease-out hover:bg-[#665571] focus:outline-none focus:ring-2 focus:ring-[#7B6886] focus:ring-offset-2"
								href="#kontakt"
							>
								Umów wizytę
							</a>
						</p>
					</div>
				</div>
			</section>

			<div class="mx-auto w-full max-w-[1200px] px-4 pb-14 pt-12 sm:px-5 sm:pb-16 sm:pt-14 md:px-7 md:pb-20 md:pt-16 lg:px-8 lg:pb-[88px] lg:pt-20 2xl:px-10">
			<section
				id="o-nas"
				class="rounded-md border border-[#e5e5ea] bg-white px-4 py-6 shadow-[0_4px_12px_rgba(0,0,0,0.04)] sm:px-5 sm:py-7 md:px-7 md:py-9 lg:p-12"
			>
				<h2
					class="mb-3 font-['Playfair_Display','Cormorant_Garamond',Georgia,serif] text-[30px] font-semibold leading-tight tracking-[-0.3px] text-[#2b2b2f] md:text-[34px] lg:text-4xl"
				>
					O nas
				</h2>
				<p>
					Świadczymy czynności notarialne z dbałością o bezpieczeństwo prawne,
					poufność oraz przejrzystą komunikację na każdym etapie współpracy.
				</p>
			</section>

			<section
				id="uslugi"
				class="mt-4 rounded-md border border-[#e5e5ea] bg-white px-4 py-6 shadow-[0_4px_12px_rgba(0,0,0,0.04)] sm:px-5 sm:py-7 md:mt-6 md:px-7 md:py-9 lg:mt-8 lg:p-12"
			>
				<h2
					class="mb-3 font-['Playfair_Display','Cormorant_Garamond',Georgia,serif] text-[30px] font-semibold leading-tight tracking-[-0.3px] text-[#2b2b2f] md:text-[34px] lg:text-4xl"
				>
					Usługi
				</h2>
				<p>
					Przygotowujemy akty notarialne, poświadczenia i pełnomocnictwa dla osób
					prywatnych oraz firm, zgodnie z najwyższymi standardami jakości.
				</p>
			</section>

			<section
				id="kontakt"
				class="mt-4 rounded-md border border-[#e5e5ea] bg-white px-4 py-6 shadow-[0_4px_12px_rgba(0,0,0,0.04)] sm:px-5 sm:py-7 md:mt-6 md:px-7 md:py-9 lg:mt-8 lg:p-12"
			>
				<h2
					class="mb-3 font-['Playfair_Display','Cormorant_Garamond',Georgia,serif] text-[30px] font-semibold leading-tight tracking-[-0.3px] text-[#2b2b2f] md:text-[34px] lg:text-4xl"
				>
					Kontakt
				</h2>
				<p>
					Skontaktuj się z nami telefonicznie lub mailowo, aby umówić termin
					spotkania dopasowany do Twoich potrzeb.
				</p>
				<ul class="mt-4 grid list-none gap-2 p-0">
					<li>
						<a
							class="inline-flex min-h-[44px] min-w-[44px] items-center rounded px-2 py-2 font-semibold text-[#7b6886] underline-offset-2 hover:text-[#665571] focus:outline-none focus:ring-2 focus:ring-[#7b6886] focus:ring-offset-2"
							href="tel:+48123456789"
						>
							+48 123 456 789
						</a>
					</li>
					<li>
						<a
							class="inline-flex min-h-[44px] min-w-[44px] items-center rounded px-2 py-2 font-semibold text-[#7b6886] underline-offset-2 hover:text-[#665571] focus:outline-none focus:ring-2 focus:ring-[#7b6886] focus:ring-offset-2"
							href="mailto:kontakt@kancelaria.pl"
						>
							kontakt@kancelaria.pl
						</a>
					</li>
					<li>
						<a
							class="inline-flex min-h-[44px] min-w-[44px] items-center rounded px-2 py-2 font-semibold text-[#7b6886] underline-offset-2 hover:text-[#665571] focus:outline-none focus:ring-2 focus:ring-[#7b6886] focus:ring-offset-2"
							href="https://maps.google.com/?q=Krakowskie+Przedmieście+10,+Warszawa"
							target="_blank"
							rel="noopener noreferrer"
						>
							Krakowskie Przedmieście 10, Warszawa
						</a>
					</li>
				</ul>
			</section>
			</div>
		</main>

		<script is:inline>
			const menuToggle = document.querySelector(".menu-toggle");
			const menuLinks = document.querySelector(".menu-links");
			const sectionLinks = Array.from(document.querySelectorAll(".menu-link"));
			const sections = Array.from(document.querySelectorAll("main section[id]"));
			const desktopMedia = window.matchMedia("(min-width: 768px)");

			const setMenuOpen = (isOpen) => {
				if (!menuToggle || !menuLinks) return;
				menuToggle.setAttribute("aria-expanded", String(isOpen));
				menuToggle.setAttribute("aria-label", isOpen ? "Zamknij menu" : "Otwórz menu");
				menuLinks.classList.toggle("hidden", !isOpen);
				menuLinks.classList.toggle("flex", isOpen);
			};

			if (menuToggle && menuLinks) {
				menuToggle.addEventListener("click", () => {
					const isOpen = menuToggle.getAttribute("aria-expanded") === "true";
					setMenuOpen(!isOpen);
				});

				sectionLinks.forEach((link) => {
					link.addEventListener("click", () => setMenuOpen(false));
				});

				desktopMedia.addEventListener("change", (event) => {
					if (event.matches) setMenuOpen(false);
				});
			}

			const setActiveLink = (id) => {
				sectionLinks.forEach((link) => {
					const isActive = link.getAttribute("href") === `#${id}`;
					link.classList.toggle("text-[#7b6886]", isActive);
					link.classList.toggle("border-[#7b6886]", isActive);
					link.classList.toggle("text-[#2b2b2f]", !isActive);
					link.classList.toggle("border-transparent", !isActive);
				});
			};

			const observer = new IntersectionObserver(
				(entries) => {
					const visibleEntry = entries
						.filter((entry) => entry.isIntersecting)
						.sort((a, b) => b.intersectionRatio - a.intersectionRatio)[0];

					if (visibleEntry) {
						setActiveLink(visibleEntry.target.id);
					}
				},
				{
					threshold: [0.45, 0.6, 0.75],
					rootMargin: "-80px 0px -45% 0px",
				}
			);

			sections.forEach((section) => observer.observe(section));
			if (sections[0]) setActiveLink(sections[0].id);
		</script>
	</body>
</html>
